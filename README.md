# AI Product Recommendation
npm install
npm run dev

Add your OpenRouter API key to .env.

The app displays products, sends the product list and user query to OpenRouter, receives recommended product IDs, and filters the local list.
